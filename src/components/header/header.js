import React from 'react';
import "./header.css";


const header = props => (
    <header className="header">
        <h1>Clicky Clack image attack!</h1>
    <h2>Click on an image to earn points, but don't click on it twice!
    </h2>
    </header>

)
export default header;